name: qwen3-asr

services:
  qwen3-asr:
    image: quantatrisk/qwen3-asr:cpu-latest
    container_name: qwen3-asr-cpu
    ports:
      - "${NGINX_PORT:-17003}:8000"
    volumes:
      - ./temp:/app/temp
      - ./logs:/app/logs
      - ./models/modelscope:/root/.cache/modelscope
      - ./models/huggingface:/root/.cache/huggingface
    environment:
      API_KEY: ${API_KEY:-}
      MODELSCOPE_PATH: /root/.cache/modelscope/hub/models
      HF_HUB_LOCAL_FILES_ONLY: ${HF_HUB_LOCAL_FILES_ONLY:-0}
      QWEN3_ASR_MODEL: ${QWEN3_ASR_MODEL:-}
      NGINX_RATE_LIMIT_RPS: ${NGINX_RATE_LIMIT_RPS:-0}
      NGINX_RATE_LIMIT_BURST: ${NGINX_RATE_LIMIT_BURST:-0}
    restart: unless-stopped
